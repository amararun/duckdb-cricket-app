{
  "name": "duckdb-cricket-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vercel dev --listen 4200",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.25",
    "typescript": "^5.6.3"
  }
}
